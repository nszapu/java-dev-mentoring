subprojects {
    apply plugin: 'java'

    version = '1.0.0'

    repositories {
        mavenCentral()
    }

    dependencies {
         testImplementation 'junit:junit:4.13.2'
    }

    test {
        testLogging {
            events "started", "skipped", "passed", "failed"
        }
    }
}